// src/components/Decrypt.jsx
import React from 'react';
import { Box, Typography } from '@mui/material';

const Decrypt = () => {
  return (
    <Box>
      <Typography variant="h5" gutterBottom>
        Decrypt Component
      </Typography>
      {/* Additional content for Decrypt */}
    </Box>
  );
};

export default Decrypt;